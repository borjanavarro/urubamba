<?php

namespace Salle\AdminBundle\Repository;

use Doctrine\ORM\EntityRepository;

/**
 * FlashRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class FlashRepository extends EntityRepository
{

	public function findAllFlash($offset, $results)
    {
        
        $query = $this->createQueryBuilder('n')
		    ->orderBy('n.fecha', 'DESC')
            ->setFirstResult($results * $offset)
            ->setMaxResults($results)
		    ->getQuery();
		 
		$flash = $query->getArrayResult();

		return $flash;
    }

    public function countFlash()
    {
    	$query = $this->createQueryBuilder('n')
    		->select('count(n.id)');

    	$count = $query->getQuery()->getSingleScalarResult();

    	return $count;
    }
}
